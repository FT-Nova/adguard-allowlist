# AdGuard Allowlist

Custom allowlist for AdGuard. Add domains here that should not be blocked.

## Format

One domain per line, without protocol or wildcards. Example:

```
example.com
sub.example.com
```

## Entries
